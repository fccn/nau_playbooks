{% if haproxy_tcp %}
  set_real_ip_from {{ openedx_nginx_admin_access_trusted_ip_cidrs }};
  real_ip_header proxy_protocol;
{% else %}
  real_ip_header X-Forwarded-For;
  set_real_ip_from {{ openedx_nginx_admin_access_trusted_ip_cidrs }};
{% endif %}