{% if haproxy_tcp %}
  set_real_ip_from 10.0.0.0/24; # default docker ingress subnet
  set_real_ip_from {{ load_balancer_hosts }};
  real_ip_header proxy_protocol
{% else %}
  real_ip_header X-Forwarded-For;
  set_real_ip_from {{ load_balancer_hosts }};
{% endif %}